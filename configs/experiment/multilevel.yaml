# Multilevel ICL experiment: coarse-to-fine PatchICLAttention.
# Loaded on top of configs/config.yaml + cluster config.
# Override from CLI: python train.py key=value  (dot-notation)

data:
  image_size: [128, 128, 128]
  resolutions:
    - [8, 8, 8]
  #  - [16, 16, 16]
  n_patches_l1: 512          # NP₁: number of sparse patches at L1
  sampling_temperature: 0.1  # Gumbel-TopK temperature (higher → more uniform sampling)
  target_sampling: predicted_entropy  # gt_previous_pred_error | gt_foreground_entropy_balanced | predicted_entropy
  context_size: 1
  max_ds_len_train: 2000
  mask_pool: avg             # how to pool masks to grid: max | avg
  synth_method: slic
  synth_unions: true
  p_synth: 1
  class_balanced: true
  use_crop: true
  random_coloring: false
  num_labels_per_sample: 1
  train_classes:
    - liver
    - lung_lower_lobe_right
    - lung_lower_lobe_left
    - lung_upper_lobe_right
    - lung_upper_lobe_left
    - lung_middle_lobe_right
    - small_bowel
    - heart
    - autochthon_left
    - autochthon_right
    - hip_left
    - hip_right
    - aorta
    - brain
    - skull
  val_classes:
    - colon
    - stomach
    - spleen
    - gluteus_maximus_right
    - gluteus_maximus_left
    - gluteus_medius_left
    - gluteus_medius_right
    - iliopsoas_left
    - iliopsoas_right
    - costal_cartilages
    - urinary_bladder
    - femur_left
    - femur_right
    - sacrum
    - kidney_left

model:
  stunet_variant: base
  stunet_pretrained: ${paths.checkpoints}/stunet/base_statedict.pt
  feature_level: all         # encoder level: int index or 'all'
  dim: 256                   # internal transformer working dim
  num_heads: 8
  num_layers: 4
  ff_factor: 2
  label_injection: additive  # additive | concat | gate | none
  output_head: linear        # linear | mlp | retrieval
  pos_encoding: sinusoidal       # none | sinusoidal | learned | rope3d
  input_norm: rmsnorm        # none | rmsnorm | l2
  dropout: 0.1
  ctx_self_attn: true
  log_n_scaling: true
  log_n_base: 512
  soft_labels_train: true   # feed fractional avg-pool values to the model during training
  soft_labels_eval: false   # binarize ctx labels at inference (cleaner retrieval signal)

train:
  loss_weights: [1.0, 1.0]   # weight for L0 and L1 losses
  epochs: 100
  batch_size: 8
  lr: 1.0e-4
  weight_decay: 1.0e-5
  workers: 18
  val_items_per_class: 30
  nd_interval: 20
  seed: 42
  aug: true
  aug_preset: nnunet
  wandb_project: patch_icl_3d_exps
  run_name: null
  checkpoint: null
  device: null
